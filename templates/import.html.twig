{% extends 'base.html.twig' %}

{% block body %}
    <h1>Import Users</h1>

    {{ form_start(form) }}
    {{ form_row(form.file) }}
    <button type="submit" class="btn btn-primary">Import</button>
    {{ form_end(form) }}
{% endblock %}