{% extends 'admin1/base.html.twig' %}


{% block title %}operation
{% endblock %}

{% block body %}
	<div class="col-xs-12">
		<h3 class="header smaller lighter blue">Supprimer operation
		</h3>
	</div>


	<table class="table">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ operation.id }}</td>
			</tr>
			<tr>
				<th>CreatedAt</th>
				<td>{{ operation.createdAt ? operation.createdAt|date('Y-m-d H:i:s') : '' }}</td>
			</tr>
		</tbody>
	</table>

	<div class="row">
		<div class="col-sm-6">
			<a href="{{ path('app_operation_index') }}" class="btn btn-primary">
				Retour à la liste
			</a>
		</div>
		<div class="col-sm-6 text-right">
			{{ include('operation/_delete_form.html.twig', {'button_text': 'Supprimer', 'button_class': 'btn btn-danger'}) }}
		</div>
	</div>
	

{% endblock %}
