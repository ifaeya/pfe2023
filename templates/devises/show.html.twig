{% extends 'admin1/base.html.twig' %}


{% block title %}devises{% endblock %}

{% block body %}
    <div class="col-xs-12">
<h3 class="header smaller lighter blue">Supprimer  devise </h3></div>


    <table class="table">
        <tbody>
            <tr>
                <th>Image</th>
                <td>{{ devise.image }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ devise.code }}</td>
            </tr>
            
            <tr>
                <th>Libelle</th>
                <td>{{ devise.libelle }}</td>
            </tr>
            <tr>
                <th>Abreviation</th>
                <td>{{ devise.abreviation }}</td>
            </tr>
            <tr>
                <th>Unite</th>
                <td>{{ devise.unite }}</td>
            </tr>
        </tbody>
    </table>

    <div class="row">
    <div class="col-sm-6">
        <a href="{{ path('app_devises_index') }}" class="btn btn-primary">
            Retour à la liste
        </a>
    </div>
    <div class="col-sm-6 text-right">
        {{ include('devises/_delete_form.html.twig', {'button_text': 'Supprimer', 'button_class': 'btn btn-danger'}) }}
    </div>
</div>
{% endblock %}
