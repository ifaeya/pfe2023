
<form method="post" action="{{ path('app_coursdevises_delete', {'id': coursdevise.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ coursdevise.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
