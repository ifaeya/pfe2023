{% extends 'admin1/base.html.twig' %}

{% block title %}devises
{% endblock %}

{% block body %}
	<div class="col-xs-12">
		<h3 class="header smaller lighter blue">Supprimer type</h3>
	</div>


	<table class="table">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ typeoperation.id }}</td>
			</tr>
			<tr>
				<th>Libelle</th>
				<td>{{ typeoperation.Libelle }}</td>
			</tr>
		</tbody>
	</table>
	<div class="row">
		<div class="col-sm-6">
			<a href="{{ path('app_typeoperation_index') }}" class="btn btn-primary">
				Retour à la liste
			</a>
		</div>
		<div class="col-sm-6 text-right">
			{{ include('typeoperation/_delete_form.html.twig', {'button_text': 'Supprimer', 'button_class': 'btn btn-danger'}) }}
		</div>
	</div>
{% endblock %}
